package geeks.day6.linklist;
/*package geeks.day6;

public class GeekslinkedListCode {
	class GFG
	{
	// function inserts the data in front of the list and returns the head of LL.
		Node insertAtBeginning(Node head1,int a1)
		{   Node node;
		   if(head1 == null){
		       node = new Node(a1);
		   }else{
		       node = new Node(a1);
		       node.next = head1;
		   }
		       return node;
		}
	// function appends the data at the end of the list and returns the head of LL.
		Node insertAtEnd(Node head1,int a1)
		{ 
		    Node node; 
		    if(head1 == null){
		        return new Node(a1);
		    }else{
		        node = head1;
		        while(node.next != null){
		            node = node.next;
		        }
		        node.next = new Node(a1);
		        return head1;
		        
		    }
		}
	}
}
*/